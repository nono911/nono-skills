interface:
  display_name: "Delivery Loop"
  short_description: "Deliver isolated changes through independent review"
  default_prompt: "Use $delivery-loop to deliver this feature in an approved worktree through independent review and verified fixes."

policy:
  allow_implicit_invocation: false
