interface:
  display_name: "Delivery Loop"
  short_description: "Deliver in isolation with native-first agents"
  default_prompt: "Use $delivery-loop to deliver this feature in approved worktrees with native subagents by default, optional explicitly selected external agents, independent review, and verified fixes."

policy:
  allow_implicit_invocation: false
