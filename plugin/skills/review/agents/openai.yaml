interface:
  display_name: "Review"
  short_description: "Find actionable defects in a code change or fix"
  default_prompt: "Use $review to find evidence-backed defects in this change without editing it."
