interface:
  display_name: "Migration"
  short_description: "Plan reversible migrations with validation and rollback"
  default_prompt: "Use $migration to design a staged, observable, and recoverable transition."
