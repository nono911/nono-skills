interface:
  display_name: "Handoff"
  short_description: "Prepare a safe continuation packet for another agent"
  default_prompt: "Use $handoff to prepare a concise continuation packet for the next agent."

policy:
  allow_implicit_invocation: false
